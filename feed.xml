<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://qwertyyuiop1234.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://qwertyyuiop1234.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-03-11T16:18:46+00:00</updated><id>https://qwertyyuiop1234.github.io/feed.xml</id><title type="html">Eren Arxiv.</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">LLM from scratch - 1.3 Multi Head Self Attention</title><link href="https://qwertyyuiop1234.github.io/blog/2026/llm-from-scratch-1-3/" rel="alternate" type="text/html" title="LLM from scratch - 1.3 Multi Head Self Attention"/><published>2026-03-08T13:00:00+00:00</published><updated>2026-03-08T13:00:00+00:00</updated><id>https://qwertyyuiop1234.github.io/blog/2026/llm-from-scratch-1-3</id><content type="html" xml:base="https://qwertyyuiop1234.github.io/blog/2026/llm-from-scratch-1-3/"><![CDATA[<h1 id="1-what-is-multi-head-self-attention">1. What Is Multi Head Self Attention?</h1> <p>Multi-head self-attention is an extension of single-head self-attention.</p> \[\begin{aligned} X&amp;: \text{Input sequence shape } (B, T, D_{model})\\ n_{head}&amp;: \text{Number of attention heads}=3 \\ D_{head} &amp;= D_{model} / n_{head}: \text{Dimension of each head} \end{aligned}\] <div class="row mt-3 justify-content-center"> <div class="col-sm-10 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/llm_from_scratch/Pasted%20image%2020260308230405-480.webp 480w,/assets/img/posts/llm_from_scratch/Pasted%20image%2020260308230405-800.webp 800w,/assets/img/posts/llm_from_scratch/Pasted%20image%2020260308230405-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/llm_from_scratch/Pasted%20image%2020260308230405.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Instead of performing a single attention operation on the whole dimension, we project the queries ($Q$), keys ($K$), and values ($V$) into $n_{head}$ different representation subspaces. We then divide the feature dimension $D_{model}$ into $n_{head}$ pieces, and perform self-attention independently on <strong>each subspace</strong>.</p> <hr/> <h1 id="2-why-multi-head">2. Why Multi Head?</h1> <p>There is a fundamental question:</p> <blockquote> <p>“Why is multi-head attention needed instead of just one large self-attention head?”</p> </blockquote> <ul> <li> <p><strong>Capturing Different Representation Subspaces</strong>: By separating the representation into $n_{head}$ distinct heads, each head can learn to focus on different types of relationships between tokens. For example, one head might attend to grammatical structures (like subject-verb relationships), another might focus on semantic meaning, and another on positional relevance.</p> </li> <li> <p><strong>Preventing Meaning Mixing</strong>: If we just use one large attention head, the attention distribution (softmax weights) will be forced to average out all these different relationships into a single weighted sum. This <strong>mixes the meanings of the tokens</strong> $\rightarrow$ resulting in a lower resolution of information. Multi-head enables multiple simultaneous attention distributions without this dilution.</p> </li> </ul> <hr/> <h1 id="3-mathematical-formulation">3. Mathematical Formulation</h1> <p>In Multi-Head Self-Attention, the operations can be summarized as:</p> <p>\(\text{MultiHead}(Q, K, V) = \text{Concat}(\text{head}_1, \dots, \text{head}_h) W^O\) \(\text{where } \text{head}_i = \text{Attention}(Q W_i^Q, K W_i^K, V W_i^V)\)</p> <ul> <li>$W_i^Q, W_i^K, W_i^V \in \mathbb{R}^{D_{model} \times D_{head}}$ are the projection matrices for head $i$.</li> <li>$W^O \in \mathbb{R}^{D_{model} \times D_{model}}$ is the final output projection matrix.</li> </ul> <hr/> <h1 id="4-code-implementation">4. Code Implementation</h1> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># from attn_mask import causal_mask
</span><span class="kn">import</span> <span class="n">math</span>
<span class="kn">import</span> <span class="n">torch</span>
<span class="kn">import</span> <span class="n">torch.nn</span> <span class="k">as</span> <span class="n">nn</span>
<span class="kn">import</span> <span class="n">torch.nn.functional</span> <span class="k">as</span> <span class="n">F</span>
  
<span class="k">class</span> <span class="nc">MultiHeadSelfAttention</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
	<span class="sh">"""</span><span class="s">1.4 Multi-head attention with explicit shape tracing.
	
	Dimensions (before masking):
		x: (B, T, d_model)
		qkv: (B, T, 3*d_model)
		view→ (B, T, 3, n_head, d_head) where d_head = d_model // n_head
		split→ q,k,v each (B, T, n_head, d_head)
		swap→ (B, n_head, T, d_head)
		scores: (B, n_head, T, T) = q @ k^T / sqrt(d_head)
		weights:(B, n_head, T, T) = softmax(scores)
		ctx: (B, n_head, T, d_head) = weights @ v
		merge: (B, T, n_head*d_head) = (B, T, d_model)
	</span><span class="sh">"""</span>
	<span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">n_head</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">d_model</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">dropout</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">0.0</span><span class="p">,</span> <span class="n">trace_shapes</span><span class="p">:</span> <span class="nb">bool</span> <span class="o">=</span> <span class="bp">True</span><span class="p">):</span>
		<span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
		<span class="k">assert</span> <span class="n">d_model</span> <span class="o">%</span> <span class="n">n_head</span> <span class="o">==</span> <span class="mi">0</span><span class="p">,</span> <span class="sh">"</span><span class="s">d_model must be divisible by n_head</span><span class="sh">"</span>
		<span class="n">self</span><span class="p">.</span><span class="n">n_head</span> <span class="o">=</span> <span class="n">n_head</span>
		<span class="n">self</span><span class="p">.</span><span class="n">d_model</span> <span class="o">=</span> <span class="n">d_model</span>
		<span class="n">self</span><span class="p">.</span><span class="n">d_head</span> <span class="o">=</span> <span class="n">d_model</span> <span class="o">//</span> <span class="n">n_head</span>
		<span class="n">self</span><span class="p">.</span><span class="n">qkv</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="mi">3</span> <span class="o">*</span> <span class="n">d_model</span><span class="p">,</span> <span class="n">bias</span> <span class="o">=</span> <span class="bp">False</span><span class="p">)</span>
		<span class="n">self</span><span class="p">.</span><span class="n">proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">d_model</span><span class="p">,</span> <span class="n">bias</span> <span class="o">=</span> <span class="bp">False</span><span class="p">)</span>
		<span class="n">self</span><span class="p">.</span><span class="n">dropout</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Dropout</span><span class="p">(</span><span class="n">dropout</span><span class="p">)</span>
		<span class="n">self</span><span class="p">.</span><span class="n">trace_shapes</span> <span class="o">=</span> <span class="n">trace_shapes</span>
	
	<span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">tensor</span><span class="p">):</span>
		<span class="n">B</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="n">D</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="n">shape</span>
		<span class="n">qkv</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">qkv</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
		<span class="n">qkv</span> <span class="o">=</span> <span class="n">qkv</span><span class="p">.</span><span class="nf">view</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">n_head</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">d_head</span><span class="p">)</span>
		<span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">trace_shapes</span><span class="p">:</span>
			<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">qkv view:</span><span class="sh">"</span><span class="p">,</span> <span class="n">qkv</span><span class="p">.</span><span class="n">shape</span><span class="p">)</span>
			
		<span class="n">q</span><span class="p">,</span> <span class="n">k</span><span class="p">,</span> <span class="n">v</span> <span class="o">=</span> <span class="n">qkv</span><span class="p">.</span><span class="nf">unbind</span><span class="p">(</span><span class="n">dim</span> <span class="o">=</span> <span class="mi">2</span><span class="p">)</span> <span class="c1"># (B, T, head, dim)
</span>		<span class="n">q</span> <span class="o">=</span> <span class="n">q</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span> <span class="c1"># (B, head, T, dim)
</span>		<span class="n">k</span> <span class="o">=</span> <span class="n">k</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>
		<span class="n">v</span> <span class="o">=</span> <span class="n">v</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>
		
		<span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">trace_shapes</span><span class="p">:</span>
			<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">q:</span><span class="sh">"</span><span class="p">,</span> <span class="n">q</span><span class="p">.</span><span class="n">shape</span><span class="p">,</span> <span class="sh">"</span><span class="s">k:</span><span class="sh">"</span><span class="p">,</span> <span class="n">k</span><span class="p">.</span><span class="n">shape</span><span class="p">,</span> <span class="sh">"</span><span class="s">v:</span><span class="sh">"</span><span class="p">,</span> <span class="n">v</span><span class="p">.</span><span class="n">shape</span><span class="p">)</span>
		
		<span class="n">scale</span> <span class="o">=</span> <span class="mf">1.0</span> <span class="o">/</span> <span class="n">math</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">k</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">])</span>
		<span class="n">attn</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">matmul</span><span class="p">(</span><span class="n">q</span><span class="p">,</span> <span class="n">k</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="o">-</span><span class="mi">2</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">))</span> <span class="o">*</span> <span class="n">scale</span> <span class="c1"># (B,heads,T,T)
</span>		
        <span class="c1"># We assume causal_mask is defined separately
</span>        <span class="c1"># mask = causal_mask(T, device = x.device)
</span>		<span class="c1"># attn = attn.masked_fill(mask, float('-inf'))
</span>		<span class="n">w</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">attn</span><span class="p">,</span> <span class="n">dim</span> <span class="o">=</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span>
		<span class="n">w</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">dropout</span><span class="p">(</span><span class="n">w</span><span class="p">)</span>
		<span class="n">ctx</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">matmul</span><span class="p">(</span><span class="n">w</span><span class="p">,</span> <span class="n">v</span><span class="p">)</span> <span class="c1"># (B,heads,T,dim)
</span>		
		<span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">trace_shapes</span><span class="p">:</span>
			<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">weights:</span><span class="sh">"</span><span class="p">,</span> <span class="n">w</span><span class="p">.</span><span class="n">shape</span><span class="p">,</span> <span class="sh">"</span><span class="s">ctx:</span><span class="sh">"</span><span class="p">,</span> <span class="n">ctx</span><span class="p">.</span><span class="n">shape</span><span class="p">)</span>
		
		<span class="n">out</span> <span class="o">=</span> <span class="n">ctx</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">).</span><span class="nf">contiguous</span><span class="p">().</span><span class="nf">view</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="n">D</span><span class="p">)</span> <span class="c1"># (B,T,d_model)
</span>		<span class="n">out</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">proj</span><span class="p">(</span><span class="n">out</span><span class="p">)</span>
		
		<span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">trace_shapes</span><span class="p">:</span>
			<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">out:</span><span class="sh">"</span><span class="p">,</span> <span class="n">out</span><span class="p">.</span><span class="n">shape</span><span class="p">)</span>
		<span class="k">return</span> <span class="n">out</span><span class="p">,</span> <span class="n">w</span>
</code></pre></div></div> <p><strong>Key Implementation Details:</strong></p> <ul> <li><strong>Fused QKV Projection</strong>: You might notice that <code class="language-plaintext highlighter-rouge">q, k, v</code> are derived from a single <code class="language-plaintext highlighter-rouge">nn.Linear(d_model, 3 * d_model)</code> rather than three separate layers. This is treated as one combined tensor primarily for <strong>the advantage of computation</strong>, allowing better GPU utilization through parallel matrix multiplication rather than being a theoretical necessity.</li> <li><strong>Final Projection Layer (<code class="language-plaintext highlighter-rouge">self.proj</code>)</strong>: After multiplying the weights and values, the outputs of all heads are concatenated together (via <code class="language-plaintext highlighter-rouge">.view(B, T, D)</code>) and passed through a final linear layer <code class="language-plaintext highlighter-rouge">self.proj</code>. This allows the model to <strong>mix</strong> the features gathered from the independent heads back together.</li> </ul> <hr/> <p><em>Reference</em> :</p> <ul> <li><a href="https://www.youtube.com/watch?v=p3sij8QzONQ&amp;t=1304s">LLMs from Scratch – Practical Engineering from Base Model to PPO RLHF</a></li> <li><a href="https://www.youtube.com/watch?v=RQowiOF_FvQ&amp;list=PLoROMvodv4rOmsNzYBMe0gJY2XS8AQg16&amp;index=8">Stanford CS231N Spring 2025 Lecture 8: Attention and Transformers</a></li> </ul>]]></content><author><name></name></author><category term="project"/><category term="study"/><category term="llm-project"/><category term="nlp"/><category term="transformer"/><category term="attention"/><summary type="html"><![CDATA[Why Multi Head Attention is important, mathematical formulations, and implementation.]]></summary></entry><entry><title type="html">LLM from scratch - 1.2 Single Head Self Attention</title><link href="https://qwertyyuiop1234.github.io/blog/2026/llm-from-scratch-1-2/" rel="alternate" type="text/html" title="LLM from scratch - 1.2 Single Head Self Attention"/><published>2026-03-08T12:00:00+00:00</published><updated>2026-03-08T12:00:00+00:00</updated><id>https://qwertyyuiop1234.github.io/blog/2026/llm-from-scratch-1-2</id><content type="html" xml:base="https://qwertyyuiop1234.github.io/blog/2026/llm-from-scratch-1-2/"><![CDATA[<h1 id="1-causal-mask">1. Causal Mask</h1> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>

<span class="k">def</span> <span class="nf">causal_mask</span><span class="p">(</span><span class="n">T</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">device</span> <span class="o">=</span> <span class="bp">None</span><span class="p">):</span>
	<span class="sh">"""</span><span class="s">Returns a bool mask where True means *masked* (disallowed).
	Shape: (1, 1, T, T) suitable for broadcasting with (B, heads, T, T).
	</span><span class="sh">"""</span>
	<span class="n">mask</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">triu</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">((</span><span class="n">T</span><span class="p">,</span><span class="n">T</span><span class="p">),</span> <span class="n">dtype</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nb">bool</span><span class="p">,</span> <span class="n">device</span> <span class="o">=</span> <span class="n">device</span><span class="p">),</span> <span class="n">diagonal</span> <span class="o">=</span> <span class="mi">1</span><span class="p">)</span>
	<span class="k">return</span> <span class="n">mask</span><span class="p">.</span><span class="nf">view</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="n">T</span><span class="p">)</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">torch.triu</code> stands for ‘triangle upper’.</li> <li><code class="language-plaintext highlighter-rouge">diagonal = 1</code> sets all elements above the main diagonal to <code class="language-plaintext highlighter-rouge">True</code> to mask future tokens.</li> </ul> <p><strong>Why use a Causal Mask?</strong> In auto-regressive models (like GPT), predicting the next word relies only on the past and present tokens. The causal mask ensures that the model cannot <strong>look ahead</strong> or <strong>cheat</strong> by giving attention to future tokens that haven’t been generated yet.</p> <div class="row mt-3 justify-content-center"> <div class="col-sm-6 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/llm_from_scratch/Pasted%20image%2020260308214938-480.webp 480w,/assets/img/posts/llm_from_scratch/Pasted%20image%2020260308214938-800.webp 800w,/assets/img/posts/llm_from_scratch/Pasted%20image%2020260308214938-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/llm_from_scratch/Pasted%20image%2020260308214938.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <hr/> <h1 id="2-what-is-single-head-self-attention">2. What Is Single Head Self Attention?</h1> <div class="row mt-3 justify-content-center"> <div class="col-sm-6 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/llm_from_scratch/Pasted%20image%2020260309002814-480.webp 480w,/assets/img/posts/llm_from_scratch/Pasted%20image%2020260309002814-800.webp 800w,/assets/img/posts/llm_from_scratch/Pasted%20image%2020260309002814-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/llm_from_scratch/Pasted%20image%2020260309002814.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Self-attention allows the model to weigh the importance of different tokens in a sequence relative to a specific token. It achieves this using three learned vectors for each token:</p> <ul> <li><strong>Query (Q)</strong>: “What kind of information am I looking for?” (The current token asking questions to context)</li> <li><strong>Key (K)</strong>: “What kind of information do I contain?” (Other tokens holding up their identity name tags)</li> <li><strong>Value (V)</strong>: “Here is my actual content.” (The underlying representation or meaning of the token)</li> </ul> <hr/> <h1 id="3-the-attention-formula">3. The Attention Formula</h1> \[Attention(Q, K, V ) = softmax( \frac{QK^T}{\sqrt{d_k}} )V\] <p><strong>Step-by-step breakdown:</strong></p> <ol> <li><strong>$QK^T$ (Calculate Scores):</strong> Take the dot product between the Query of the current token and the Keys of all other tokens. A high dot product means the Key closely matches what the Query is looking for (high relevance).</li> <li><strong>$\frac{1}{\sqrt{d_k}}$ (Scale):</strong> If the dimension $d_k$ is large, the dot products can grow extremely large or small. This causes the gradients of the Softmax function to <strong>vanish</strong> (become flat). Dividing by $\sqrt{d_k}$ keeps <strong>the variance stable</strong>.</li> <li><strong>$softmax(\dots)$ (Get Weights):</strong> Converts the raw scores into probabilities (Attention Weights) that sum to 1.</li> <li><strong>$\times V$ (Get Context):</strong> Multiplies the Attention Weights by the Value vectors. The result is a weighted sum representing the newly contextualized token.</li> </ol> <hr/> <h1 id="4-code-implementation">4. Code Implementation</h1> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">math</span>
<span class="kn">import</span> <span class="n">torch</span>
<span class="kn">import</span> <span class="n">torch.nn</span> <span class="k">as</span> <span class="n">nn</span>
<span class="kn">import</span> <span class="n">torch.nn.functional</span> <span class="k">as</span> <span class="n">F</span>
<span class="c1"># from attn_mask import causal_mask
</span>
<span class="k">class</span> <span class="nc">SingleHeadSelfAttention</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
	<span class="sh">"""</span><span class="s">1.3 Single-head attention (explicit shapes).
	
	Dimensions summary (single head):
		X: (B, T, d_model)
		q, k, v: (B, T, d_k)
		scores: (B, T, T)
		Weights: (B, T, T)
		Output: (B, T, d_k)
	</span><span class="sh">"""</span>
	<span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">d_model</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">d_k</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">dropout</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">0.0</span><span class="p">,</span> <span class="n">trace_shapes</span><span class="p">:</span> <span class="nb">bool</span> <span class="o">=</span> <span class="bp">False</span><span class="p">):</span>
		<span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
		<span class="n">self</span><span class="p">.</span><span class="n">q</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">d_k</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
		<span class="n">self</span><span class="p">.</span><span class="n">k</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">d_k</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
		<span class="n">self</span><span class="p">.</span><span class="n">v</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">d_k</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
		<span class="n">self</span><span class="p">.</span><span class="n">dropout</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Dropout</span><span class="p">(</span><span class="n">dropout</span><span class="p">)</span>
		<span class="n">self</span><span class="p">.</span><span class="n">trace_shapes</span> <span class="o">=</span> <span class="n">trace_shapes</span>
	
	<span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">):</span> <span class="c1"># x: (B, T, d_model)
</span>		<span class="n">B</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="n">shape</span>
		<span class="n">q</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">q</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="c1"># (B,T,d_k)
</span>		<span class="n">k</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">k</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="c1"># (B,T,d_k)
</span>		<span class="n">v</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">v</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="c1"># (B,T,d_k)
</span>		<span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">trace_shapes</span><span class="p">:</span>
			<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">q </span><span class="si">{</span><span class="n">q</span><span class="p">.</span><span class="n">shape</span><span class="si">}</span><span class="s"> k </span><span class="si">{</span><span class="n">k</span><span class="p">.</span><span class="n">shape</span><span class="si">}</span><span class="s"> v </span><span class="si">{</span><span class="n">v</span><span class="p">.</span><span class="n">shape</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
			
		<span class="n">scale</span> <span class="o">=</span> <span class="mf">1.0</span> <span class="o">/</span> <span class="n">math</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">q</span><span class="p">.</span><span class="nf">size</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">))</span>
		<span class="n">attn</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">matmul</span><span class="p">(</span><span class="n">q</span><span class="p">,</span> <span class="n">k</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="o">-</span><span class="mi">2</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">))</span> <span class="o">*</span> <span class="n">scale</span> <span class="c1"># (B,T,T)
</span>		<span class="n">mask</span> <span class="o">=</span> <span class="nf">causal_mask</span><span class="p">(</span><span class="n">T</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">x</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
		<span class="n">attn</span> <span class="o">=</span> <span class="n">attn</span><span class="p">.</span><span class="nf">masked_fill</span><span class="p">(</span><span class="n">mask</span><span class="p">.</span><span class="nf">squeeze</span><span class="p">(</span><span class="mi">1</span><span class="p">),</span> <span class="nf">float</span><span class="p">(</span><span class="sh">'</span><span class="s">-inf</span><span class="sh">'</span><span class="p">))</span>
		<span class="n">w</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">attn</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
		<span class="n">w</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">dropout</span><span class="p">(</span><span class="n">w</span><span class="p">)</span>
		<span class="n">out</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">matmul</span><span class="p">(</span><span class="n">w</span><span class="p">,</span> <span class="n">v</span><span class="p">)</span> <span class="c1"># (B,T,d_k)
</span>		
		<span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">trace_shapes</span><span class="p">:</span>
			<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">weights </span><span class="si">{</span><span class="n">w</span><span class="p">.</span><span class="n">shape</span><span class="si">}</span><span class="s"> out </span><span class="si">{</span><span class="n">out</span><span class="p">.</span><span class="n">shape</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
		<span class="k">return</span> <span class="n">out</span><span class="p">,</span> <span class="n">w</span>
</code></pre></div></div> <hr/> <p><em>Reference</em> :</p> <ul> <li><a href="https://www.youtube.com/watch?v=p3sij8QzONQ&amp;t=1304s">LLMs from Scratch – Practical Engineering from Base Model to PPO RLHF</a></li> <li><a href="https://www.youtube.com/watch?v=RQowiOF_FvQ&amp;list=PLoROMvodv4rOmsNzYBMe0gJY2XS8AQg16&amp;index=8">Stanford CS231N Spring 2025 Lecture 8: Attention and Transformers</a></li> </ul>]]></content><author><name></name></author><category term="project"/><category term="study"/><category term="llm-project"/><category term="nlp"/><category term="transformer"/><category term="attention"/><summary type="html"><![CDATA[Implementation details of single head self attention and causal masks.]]></summary></entry><entry><title type="html">LLM from scratch - 1.1 Positional Encoding</title><link href="https://qwertyyuiop1234.github.io/blog/2026/llm-from-scratch-1-1/" rel="alternate" type="text/html" title="LLM from scratch - 1.1 Positional Encoding"/><published>2026-03-08T11:00:00+00:00</published><updated>2026-03-08T11:00:00+00:00</updated><id>https://qwertyyuiop1234.github.io/blog/2026/llm-from-scratch-1-1</id><content type="html" xml:base="https://qwertyyuiop1234.github.io/blog/2026/llm-from-scratch-1-1/"><![CDATA[<h1 id="1-positional-encoding">1. Positional Encoding</h1> <div class="row mt-3 justify-content-center"> <div class="col-sm-6 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/llm_from_scratch/Pasted%20image%2020260308021220-480.webp 480w,/assets/img/posts/llm_from_scratch/Pasted%20image%2020260308021220-800.webp 800w,/assets/img/posts/llm_from_scratch/Pasted%20image%2020260308021220-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/llm_from_scratch/Pasted%20image%2020260308021220.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>According to the transformer architecture, the positional encoding is used for indexing the order on tokens.</p> <p>Let the input $x: (\text{B, T, D})$</p> <p>which:</p> <ul> <li>$B$: batch</li> <li>$T$: timestep / sequence length</li> <li>$D$: dimension</li> </ul> <h2 id="11-learned-postional-encoding">1.1 Learned Postional Encoding</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">math</span>
<span class="kn">import</span> <span class="n">torch</span>
<span class="kn">import</span> <span class="n">torch.nn</span> <span class="k">as</span> <span class="n">nn</span>

<span class="k">class</span> <span class="nc">LearnedPositionalEncoding</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
	<span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">max_len</span> <span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">d_model</span> <span class="p">:</span> <span class="nb">int</span><span class="p">):</span>
		<span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
		<span class="n">self</span><span class="p">.</span><span class="n">emb</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">max_len</span><span class="p">,</span> <span class="n">d_model</span><span class="p">)</span>
		
	<span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span> <span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">tensor</span><span class="p">):</span>
		<span class="c1"># B: Batch size, T: Time steps / sequence length
</span>		<span class="n">B</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="n">shape</span>
		<span class="n">pos</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="n">T</span><span class="p">,</span> <span class="n">device</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
		<span class="n">pos_emb</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">emb</span><span class="p">(</span><span class="n">pos</span><span class="p">)</span> <span class="c1"># get pos_emb about pos (T, d_model)
</span>		<span class="k">return</span> <span class="n">x</span> <span class="o">+</span> <span class="n">pos_emb</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span> <span class="c1"># fit the dimension and broadcast over the batch
</span></code></pre></div></div> <p>You might be wondering why we use <code class="language-plaintext highlighter-rouge">nn.Embedding</code> instead of <code class="language-plaintext highlighter-rouge">nn.Linear</code>, since both can conceptually serve as a learned linear mapping. The key difference lies in <strong>how they access data</strong> under the hood:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">nn.Linear</code></strong>: Requires the input to be a one-hot encoded vector, and performs a full matrix multiplication.</li> <li><strong><code class="language-plaintext highlighter-rouge">nn.Embedding</code></strong>: Acts as a simple <strong>Lookup Table</strong>. It takes an integer index and directly retrieves the corresponding vector from memory.</li> </ul> <p>Since position IDs are just discrete integers ($0, 1, 2, …$), using one-hot encoding followed by matrix multiplication (<code class="language-plaintext highlighter-rouge">nn.Linear</code>) would be highly inefficient and wasteful. <code class="language-plaintext highlighter-rouge">nn.Embedding</code> provides a much faster and memory-efficient way to grab the specific positional vector we need.</p> <blockquote> <p><strong>💡 What does <code class="language-plaintext highlighter-rouge">device=x.device</code> mean?</strong></p> <p>In PyTorch, data can reside on either the CPU or a specific GPU. Operations can only be performed if all involved tensors are on the <strong>same device</strong>. By passing <code class="language-plaintext highlighter-rouge">device=x.device</code>, we ensure that our newly created position indices (<code class="language-plaintext highlighter-rouge">pos</code>) are generated on the exact same hardware accelerator where our input tensor <code class="language-plaintext highlighter-rouge">x</code> currently lives, preventing device mismatch errors.</p> </blockquote> <h2 id="12-sinusoidal--postional-encoding">1.2 Sinusoidal Postional Encoding</h2> <p>This method was proposed in the <em>Attention Is All You Need</em> paper.</p> <div class="row mt-3 justify-content-center"> <div class="col-sm-10 mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/llm_from_scratch/Pasted%20image%2020260308025153-480.webp 480w,/assets/img/posts/llm_from_scratch/Pasted%20image%2020260308025153-800.webp 800w,/assets/img/posts/llm_from_scratch/Pasted%20image%2020260308025153-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/llm_from_scratch/Pasted%20image%2020260308025153.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">SinusoidalPositionalEncoding</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
	<span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">max_len</span> <span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">d_model</span> <span class="p">:</span> <span class="nb">int</span><span class="p">):</span>
		<span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
		<span class="n">pe</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">max_len</span><span class="p">,</span> <span class="n">d_model</span><span class="p">)</span>
		<span class="n">pos</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">max_len</span><span class="p">,</span> <span class="n">dtype</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nb">float</span><span class="p">).</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>
		<span class="n">denom</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">((</span><span class="o">-</span><span class="n">math</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="mf">10000.0</span><span class="p">)</span> <span class="o">/</span> <span class="n">d_model</span><span class="p">)</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">d_model</span><span class="p">,</span> <span class="mi">2</span><span class="p">).</span><span class="nf">float</span><span class="p">())</span>
		<span class="n">pe</span><span class="p">[:,</span> <span class="mi">0</span><span class="p">::</span><span class="mi">2</span><span class="p">]</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="n">pos</span> <span class="o">*</span> <span class="n">denom</span><span class="p">)</span>
		<span class="n">pe</span><span class="p">[:,</span> <span class="mi">1</span><span class="p">::</span><span class="mi">2</span><span class="p">]</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="n">pos</span> <span class="o">*</span> <span class="n">denom</span><span class="p">)</span>
		<span class="n">self</span><span class="p">.</span><span class="nf">register_buffer</span><span class="p">(</span><span class="sh">'</span><span class="s">pe</span><span class="sh">'</span><span class="p">,</span> <span class="n">pe</span><span class="p">)</span> <span class="c1"># register pe at buffer for some reasons
</span>  
	<span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span> <span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">tensor</span><span class="p">):</span>
	    <span class="n">B</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="n">shape</span>
	    <span class="k">return</span> <span class="n">x</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="n">pe</span><span class="p">[:</span><span class="n">T</span><span class="p">].</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>💡 Why implement the denominator using $\log$ and $\exp$?</strong></p> <p>The formula for the denominator in the paper is $10000^{2i / d_{model}}$. Directly calculating $10000^{x}$ for large exponents can cause severe <strong>floating-point precision issues</strong> (overflow/underflow) in computers.<br/> By utilizing the mathematical property $x = \exp(\log(x))$, we can rewrite $10000^{2i / d} $ as: $\exp(\frac{2i}{d} \cdot \log(10000))$. This computes the same exact values but remains highly <strong>numerically stable</strong> during tensor operations.</p> </blockquote> <h3 id="121-deep-dive-into-sinusoidal-positional-encoding">1.2.1 Deep Dive Into Sinusoidal Positional Encoding</h3> <p>Look at the image above and you’ll notice it uses $\sin$ and $\cos$ functions. But why exactly these functions?</p> <p>Sinusoidal positional encoding identifies a token’s position by mapping it to a <strong>unique pattern</strong> of sine and cosine values across multiple dimensions.</p> <p>Think of it like the <strong>hands of a clock</strong>:</p> <ul> <li>The first few dimensions (high frequency) move very fast, like the second hand, capturing exact local positions.</li> <li>The later dimensions (low frequency) move very slowly, like the hour hand, giving a sense of the broader, long-range position in the sequence.</li> </ul> <p>Because each dimension pulses at a <strong>different frequency</strong>, the overall combination acts as a <strong>distinct, continuous signature</strong> for that specific position. This continuous nature is crucial because it allows the model to easily calculate <strong>relative distances</strong> between tokens. Because of trigonometric properties (like $\sin(\alpha + \beta)$), a positional offset can be computed as a simple linear transformation, making it incredibly easy for the attention mechanism to learn relative positions.</p> <p>The constant $10000$ is simply a design choice (hyperparameter) that sets the maximum wavelength, ensuring the “hour hand” doesn’t complete a full cycle even for very long sequences.</p> <p>The term $2i / d_{model}$ spaces these frequencies smoothly on a <strong>logarithmic scale</strong>, ensuring the model captures positional patterns at various resolutions from short-range to long-range.</p> <hr/> <p><em>Reference</em> :</p> <ul> <li><a href="https://www.youtube.com/watch?v=p3sij8QzONQ&amp;t=1304s">LLMs from Scratch – Practical Engineering from Base Model to PPO RLHF</a></li> </ul>]]></content><author><name></name></author><category term="project"/><category term="study"/><category term="llm-project"/><category term="nlp"/><category term="transformer"/><summary type="html"><![CDATA[Positional encoding explanations and code implementations, including learned and sinusoidal encoding.]]></summary></entry><entry><title type="html">Deep Dive into MicroGPT by Karpathy</title><link href="https://qwertyyuiop1234.github.io/blog/2026/microgpt-karpathy/" rel="alternate" type="text/html" title="Deep Dive into MicroGPT by Karpathy"/><published>2026-02-17T17:00:00+00:00</published><updated>2026-02-17T17:00:00+00:00</updated><id>https://qwertyyuiop1234.github.io/blog/2026/microgpt-karpathy</id><content type="html" xml:base="https://qwertyyuiop1234.github.io/blog/2026/microgpt-karpathy/"><![CDATA[<p>In this post, I’m reviewing <a href="https://karpathy.github.io/2026/02/12/microgpt/" target="\_blank"><strong>MicroGPT by Andrej Karpathy</strong></a>, which is an essential resource for anyone interested in understanding how language models work from scratch.</p> <hr/> <h1 id="1-getting-datasets--tokenizer">1. Getting Datasets &amp; Tokenizer</h1> <h2 id="11-download-dataset">1.1 Download Dataset</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Let there be a Dataset `docs`: list[str] of documents (e.g. a list of names)
</span><span class="k">if</span> <span class="ow">not</span> <span class="n">os</span><span class="p">.</span><span class="n">path</span><span class="p">.</span><span class="nf">exists</span><span class="p">(</span><span class="sh">'</span><span class="s">input.txt</span><span class="sh">'</span><span class="p">):</span>
    <span class="kn">import</span> <span class="n">urllib.request</span>
    <span class="n">names_url</span> <span class="o">=</span> <span class="sh">'</span><span class="s">https://raw.githubusercontent.com/karpathy/makemore/988aa59/names.txt</span><span class="sh">'</span>
    <span class="n">urllib</span><span class="p">.</span><span class="n">request</span><span class="p">.</span><span class="nf">urlretrieve</span><span class="p">(</span><span class="n">names_url</span><span class="p">,</span> <span class="sh">'</span><span class="s">input.txt</span><span class="sh">'</span><span class="p">)</span>
<span class="n">docs</span> <span class="o">=</span> <span class="p">[</span><span class="n">line</span><span class="p">.</span><span class="nf">strip</span><span class="p">()</span> <span class="k">for</span> <span class="n">line</span> <span class="ow">in</span> <span class="nf">open</span><span class="p">(</span><span class="sh">'</span><span class="s">input.txt</span><span class="sh">'</span><span class="p">)</span> <span class="k">if</span> <span class="n">line</span><span class="p">.</span><span class="nf">strip</span><span class="p">()]</span>
<span class="n">random</span><span class="p">.</span><span class="nf">shuffle</span><span class="p">(</span><span class="n">docs</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">num docs: </span><span class="si">{</span><span class="nf">len</span><span class="p">(</span><span class="n">docs</span><span class="p">)</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>  <span class="c1"># total number of names is 32,033
</span></code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">urllib.request</code> is a Python standard library module for fetching data from URLs.</p> <p>The dataset consists of 32,033 human names, each on a separate line. After loading, the names are stripped of whitespace and shuffled randomly.</p> <p><br/></p> <h2 id="12-tokenizer">1.2 Tokenizer</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Let there be a Tokenizer to translate strings to sequences of integers ("tokens") and back
</span><span class="n">uchars</span> <span class="o">=</span> <span class="nf">sorted</span><span class="p">(</span><span class="nf">set</span><span class="p">(</span><span class="sh">''</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">docs</span><span class="p">)))</span>  <span class="c1"># unique characters in the dataset become token ids 0..n-1
</span><span class="n">BOS</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">uchars</span><span class="p">)</span>  <span class="c1"># token id for a special Beginning of Sequence (BOS) token
</span><span class="n">vocab_size</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">uchars</span><span class="p">)</span> <span class="o">+</span> <span class="mi">1</span>  <span class="c1"># total number of unique tokens, +1 is for BOS
</span><span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">vocab size: </span><span class="si">{</span><span class="n">vocab_size</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">uchars</code> is the sorted set of all unique characters that appear across the names in <code class="language-plaintext highlighter-rouge">input.txt</code>. Each character is assigned a token ID from <code class="language-plaintext highlighter-rouge">0</code> to <code class="language-plaintext highlighter-rouge">n-1</code>.</li> <li><code class="language-plaintext highlighter-rouge">vocab_size</code> is <code class="language-plaintext highlighter-rouge">len(uchars) + 1</code> because we need one additional ID for the <strong>BOS</strong> token. Every token — including special tokens — requires its own unique ID in the vocabulary.</li> </ul> <blockquote> <p><strong>What is BOS (Beginning of Sequence)?</strong></p> <p>BOS is a special token that marks the start of a sequence. During <strong>generation</strong>, the model receives the BOS token as its initial input to begin producing the first real character. Without BOS, the model would have no starting signal and could not initiate generation.</p> <p>In this character-level model, BOS is the <em>only</em> special token — and it <strong>doubles as the EOS (End of Sequence) token</strong>. Each name is wrapped like this:</p> <p><code class="language-plaintext highlighter-rouge">[BOS] e m m a [BOS]</code></p> <p>The same token appears at both the beginning and the end. During generation, when the model predicts BOS as the next token, that signals the name is complete and generation stops. This is an elegant design: a single special token handles both roles, keeping the vocabulary minimal.</p> </blockquote> <p>For a deeper understanding of tokenization strategies, see: <a href="https://nebius.com/blog/posts/how-tokenizers-work-in-ai-models" target="\_blank">How Tokenizers Work in AI Models (Nebius)</a></p> <hr/> <h1 id="2-computation-graph">2. Computation Graph</h1> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Let there be Autograd to recursively apply the chain rule through a computation graph
</span><span class="k">class</span> <span class="nc">Value</span><span class="p">:</span>
    <span class="n">__slots__</span> <span class="o">=</span> <span class="p">(</span><span class="sh">'</span><span class="s">data</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">grad</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">_children</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">_local_grads</span><span class="sh">'</span><span class="p">)</span>
</code></pre></div></div> <p><br/></p> <h2 id="21-why-__slots__">2.1 Why <code class="language-plaintext highlighter-rouge">__slots__</code>?</h2> <p>In a standard Python class, instance attributes are stored in a per-instance <code class="language-plaintext highlighter-rouge">__dict__</code> dictionary. This is flexible but comes with overhead: each dictionary maintains a hash table, keys, values, and internal bookkeeping.</p> <p><strong>Without <code class="language-plaintext highlighter-rouge">__slots__</code></strong> (default):</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>instance
  └── __dict__  (a full hash-table dictionary)
        ├── key → value
        ├── key → value
        └── ...
</code></pre></div></div> <p><strong>With <code class="language-plaintext highlighter-rouge">__slots__</code></strong>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>instance
  └── fixed attribute slots  (C-struct-like, no dictionary)
        ├── data
        ├── grad
        ├── _children
        └── _local_grads
</code></pre></div></div> <p>By declaring <code class="language-plaintext highlighter-rouge">__slots__</code>, Python allocates a <strong>fixed-size struct</strong> for the listed attributes instead of a dictionary. This:</p> <ul> <li><strong>Saves memory</strong> — no dictionary overhead per instance (important when creating thousands of <code class="language-plaintext highlighter-rouge">Value</code> nodes in a computation graph).</li> <li><strong>Speeds up attribute access</strong> — direct offset-based lookup instead of hash-table lookup.</li> <li><strong>Prevents accidental attribute creation</strong> — assigning to a non-declared attribute raises <code class="language-plaintext highlighter-rouge">AttributeError</code>.</li> </ul> <p><br/></p> <h2 id="22-special-methods">2.2 Special Methods</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">data</span><span class="p">,</span> <span class="n">children</span><span class="o">=</span><span class="p">(),</span> <span class="n">local_grads</span><span class="o">=</span><span class="p">()):</span>
    <span class="n">self</span><span class="p">.</span><span class="n">data</span> <span class="o">=</span> <span class="n">data</span>                <span class="c1"># scalar value of this node (computed during forward pass)
</span>    <span class="n">self</span><span class="p">.</span><span class="n">grad</span> <span class="o">=</span> <span class="mi">0</span>                   <span class="c1"># derivative of loss w.r.t. this node (computed during backward pass)
</span>    <span class="n">self</span><span class="p">.</span><span class="n">_children</span> <span class="o">=</span> <span class="n">children</span>       <span class="c1"># children of this node in the computation graph
</span>    <span class="n">self</span><span class="p">.</span><span class="n">_local_grads</span> <span class="o">=</span> <span class="n">local_grads</span> <span class="c1"># local derivatives of this node w.r.t. its children
</span>
<span class="k">def</span> <span class="nf">__add__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">other</span><span class="p">):</span>
    <span class="n">other</span> <span class="o">=</span> <span class="n">other</span> <span class="k">if</span> <span class="nf">isinstance</span><span class="p">(</span><span class="n">other</span><span class="p">,</span> <span class="n">Value</span><span class="p">)</span> <span class="k">else</span> <span class="nc">Value</span><span class="p">(</span><span class="n">other</span><span class="p">)</span>
    <span class="k">return</span> <span class="nc">Value</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">data</span> <span class="o">+</span> <span class="n">other</span><span class="p">.</span><span class="n">data</span><span class="p">,</span> <span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">other</span><span class="p">),</span> <span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">))</span>

<span class="k">def</span> <span class="nf">__mul__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">other</span><span class="p">):</span>
    <span class="n">other</span> <span class="o">=</span> <span class="n">other</span> <span class="k">if</span> <span class="nf">isinstance</span><span class="p">(</span><span class="n">other</span><span class="p">,</span> <span class="n">Value</span><span class="p">)</span> <span class="k">else</span> <span class="nc">Value</span><span class="p">(</span><span class="n">other</span><span class="p">)</span>
    <span class="k">return</span> <span class="nc">Value</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">data</span> <span class="o">*</span> <span class="n">other</span><span class="p">.</span><span class="n">data</span><span class="p">,</span> <span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">other</span><span class="p">),</span> <span class="p">(</span><span class="n">other</span><span class="p">.</span><span class="n">data</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">data</span><span class="p">))</span>

<span class="k">def</span> <span class="nf">__pow__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">other</span><span class="p">):</span> <span class="k">return</span> <span class="nc">Value</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">data</span><span class="o">**</span><span class="n">other</span><span class="p">,</span> <span class="p">(</span><span class="n">self</span><span class="p">,),</span> <span class="p">(</span><span class="n">other</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">data</span><span class="o">**</span><span class="p">(</span><span class="n">other</span><span class="o">-</span><span class="mi">1</span><span class="p">),))</span>
<span class="k">def</span> <span class="nf">log</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>  <span class="k">return</span> <span class="nc">Value</span><span class="p">(</span><span class="n">math</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">data</span><span class="p">),</span> <span class="p">(</span><span class="n">self</span><span class="p">,),</span> <span class="p">(</span><span class="mi">1</span><span class="o">/</span><span class="n">self</span><span class="p">.</span><span class="n">data</span><span class="p">,))</span>
<span class="k">def</span> <span class="nf">exp</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>  <span class="k">return</span> <span class="nc">Value</span><span class="p">(</span><span class="n">math</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">data</span><span class="p">),</span> <span class="p">(</span><span class="n">self</span><span class="p">,),</span> <span class="p">(</span><span class="n">math</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">data</span><span class="p">),))</span>
<span class="k">def</span> <span class="nf">relu</span><span class="p">(</span><span class="n">self</span><span class="p">):</span> <span class="k">return</span> <span class="nc">Value</span><span class="p">(</span><span class="nf">max</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">data</span><span class="p">),</span> <span class="p">(</span><span class="n">self</span><span class="p">,),</span> <span class="p">(</span><span class="nf">float</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">data</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">),))</span>
<span class="k">def</span> <span class="nf">__neg__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>      <span class="k">return</span> <span class="n">self</span> <span class="o">*</span> <span class="o">-</span><span class="mi">1</span>
<span class="k">def</span> <span class="nf">__radd__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">other</span><span class="p">):</span> <span class="k">return</span> <span class="n">self</span> <span class="o">+</span> <span class="n">other</span>
<span class="k">def</span> <span class="nf">__sub__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">other</span><span class="p">):</span>  <span class="k">return</span> <span class="n">self</span> <span class="o">+</span> <span class="p">(</span><span class="o">-</span><span class="n">other</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">__rsub__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">other</span><span class="p">):</span> <span class="k">return</span> <span class="n">other</span> <span class="o">+</span> <span class="p">(</span><span class="o">-</span><span class="n">self</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">__rmul__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">other</span><span class="p">):</span> <span class="k">return</span> <span class="n">self</span> <span class="o">*</span> <span class="n">other</span>
<span class="k">def</span> <span class="nf">__truediv__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">other</span><span class="p">):</span>  <span class="k">return</span> <span class="n">self</span> <span class="o">*</span> <span class="n">other</span><span class="o">**-</span><span class="mi">1</span>
<span class="k">def</span> <span class="nf">__rtruediv__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">other</span><span class="p">):</span> <span class="k">return</span> <span class="n">other</span> <span class="o">*</span> <span class="n">self</span><span class="o">**-</span><span class="mi">1</span>
</code></pre></div></div> <p>Each operation creates a new <code class="language-plaintext highlighter-rouge">Value</code> node that records:</p> <ol> <li>The <strong>forward result</strong> (<code class="language-plaintext highlighter-rouge">data</code>)</li> <li>Its <strong>parent operands</strong> (<code class="language-plaintext highlighter-rouge">_children</code>)</li> <li>The <strong>local gradients</strong> (<code class="language-plaintext highlighter-rouge">_local_grads</code>) — partial derivatives of this operation’s output with respect to each child</li> </ol> <p>This builds a <strong>computation graph</strong> during the forward pass that will later be traversed in reverse for backpropagation.</p> <p><br/></p> <h3 id="221-normal-methods-vs-reflected-methods">2.2.1 Normal Methods vs. Reflected Methods</h3> <p>Consider <code class="language-plaintext highlighter-rouge">__add__</code> and <code class="language-plaintext highlighter-rouge">__radd__</code>. Why do we need the reflected (reverse) version?</p> <p>When Python evaluates <code class="language-plaintext highlighter-rouge">3 + Value(5)</code>:</p> <ol> <li>Python first tries <code class="language-plaintext highlighter-rouge">int.__add__(3, Value(5))</code> → <code class="language-plaintext highlighter-rouge">int</code> doesn’t know how to add a <code class="language-plaintext highlighter-rouge">Value</code>, so it returns <code class="language-plaintext highlighter-rouge">NotImplemented</code>.</li> <li>Python then falls back to <code class="language-plaintext highlighter-rouge">Value.__radd__(Value(5), 3)</code> → this works because <code class="language-plaintext highlighter-rouge">__radd__</code> calls <code class="language-plaintext highlighter-rouge">self + other</code>, which triggers <code class="language-plaintext highlighter-rouge">Value.__add__</code>.</li> </ol> <p>Without <code class="language-plaintext highlighter-rouge">__radd__</code>, expressions like <code class="language-plaintext highlighter-rouge">3 + Value(5)</code> or <code class="language-plaintext highlighter-rouge">2 * Value(3)</code> would raise a <code class="language-plaintext highlighter-rouge">TypeError</code>.</p> <p><br/></p> <h3 id="222-why-__sub__-uses---other-instead-of-direct-subtraction">2.2.2 Why <code class="language-plaintext highlighter-rouge">__sub__</code> Uses <code class="language-plaintext highlighter-rouge">+ (-other)</code> Instead of Direct Subtraction</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">__sub__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">other</span><span class="p">):</span> <span class="k">return</span> <span class="n">self</span> <span class="o">+</span> <span class="p">(</span><span class="o">-</span><span class="n">other</span><span class="p">)</span>
</code></pre></div></div> <p>This might look roundabout, but it’s a deliberate design choice:</p> <ul> <li><strong>Reuses existing operations</strong>: Subtraction is decomposed into negation (<code class="language-plaintext highlighter-rouge">__neg__</code>) and addition (<code class="language-plaintext highlighter-rouge">__add__</code>), both of which are already implemented with correct gradient tracking.</li> <li><strong>Avoids redundant gradient logic</strong>: If subtraction were implemented as a separate operation, we would need to define and maintain additional local gradient formulas.</li> <li><strong>Keeps the computation graph simple</strong>: Every operation in the graph maps to one of a small set of primitives (<code class="language-plaintext highlighter-rouge">add</code>, <code class="language-plaintext highlighter-rouge">mul</code>, <code class="language-plaintext highlighter-rouge">pow</code>, <code class="language-plaintext highlighter-rouge">log</code>, <code class="language-plaintext highlighter-rouge">exp</code>, <code class="language-plaintext highlighter-rouge">relu</code>), making the backward pass straightforward.</li> </ul> <p><br/></p> <hr/> <p><em>This post will be updated as I continue studying the remaining sections of MicroGPT.</em></p>]]></content><author><name></name></author><category term="AI"/><category term="NLP"/><category term="microgpt"/><category term="karpathy"/><category term="autograd"/><category term="backpropagation"/><category term="nlp"/><summary type="html"><![CDATA[A detailed walkthrough of Karpathy's MicroGPT, covering dataset preparation, character-level tokenization, a minimal autograd engine (the Value class), Python special methods, and backpropagation via topological sort.]]></summary></entry><entry><title type="html">[CS231n] Assignment 1 - Q2. Implement a Softmax Classifier</title><link href="https://qwertyyuiop1234.github.io/blog/2026/cs231n-softmax/" rel="alternate" type="text/html" title="[CS231n] Assignment 1 - Q2. Implement a Softmax Classifier"/><published>2026-02-16T12:00:00+00:00</published><updated>2026-02-16T12:00:00+00:00</updated><id>https://qwertyyuiop1234.github.io/blog/2026/cs231n-softmax</id><content type="html" xml:base="https://qwertyyuiop1234.github.io/blog/2026/cs231n-softmax/"><![CDATA[<h1 id="1-preprocessing-datasets">1. Preprocessing Datasets</h1> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># preprocessing: subtract the mean image
# first: compute the image mean based on the training data
</span><span class="n">mean_image</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">mean</span><span class="p">(</span><span class="n">X_train</span><span class="p">,</span> <span class="n">axis</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="n">mean_image</span><span class="p">[:</span><span class="mi">10</span><span class="p">])</span> <span class="c1"># print a few of the elements
</span><span class="n">plt</span><span class="p">.</span><span class="nf">figure</span><span class="p">(</span><span class="n">figsize</span><span class="o">=</span><span class="p">(</span><span class="mi">4</span><span class="p">,</span><span class="mi">4</span><span class="p">))</span>
<span class="n">plt</span><span class="p">.</span><span class="nf">imshow</span><span class="p">(</span><span class="n">mean_image</span><span class="p">.</span><span class="nf">reshape</span><span class="p">((</span><span class="mi">32</span><span class="p">,</span><span class="mi">32</span><span class="p">,</span><span class="mi">3</span><span class="p">)).</span><span class="nf">astype</span><span class="p">(</span><span class="sh">'</span><span class="s">uint8</span><span class="sh">'</span><span class="p">))</span> <span class="c1"># visualize the mean image
</span><span class="n">plt</span><span class="p">.</span><span class="nf">show</span><span class="p">()</span>

<span class="c1"># second: subtract the mean image from train and test data
</span><span class="n">X_train</span> <span class="o">-=</span> <span class="n">mean_image</span>
<span class="n">X_val</span> <span class="o">-=</span> <span class="n">mean_image</span>
<span class="n">X_test</span> <span class="o">-=</span> <span class="n">mean_image</span>
<span class="n">X_dev</span> <span class="o">-=</span> <span class="n">mean_image</span>
</code></pre></div></div> <p>We have already flattened the <code class="language-plaintext highlighter-rouge">X_train</code> dataset in the previous code, so <code class="language-plaintext highlighter-rouge">X_train</code> $\in \mathbb{R}^{49000 \times 3072}$.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/cs231n_softmax/Pasted%20image%2020260215223443-480.webp 480w,/assets/img/posts/cs231n_softmax/Pasted%20image%2020260215223443-800.webp 800w,/assets/img/posts/cs231n_softmax/Pasted%20image%2020260215223443-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/cs231n_softmax/Pasted%20image%2020260215223443.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <blockquote> <p><em>Why subtract the mean image from the dataset?</em></p> <ul> <li><strong>Center the data</strong>: Makes the average of each feature zero. <ul> <li>Stabilizes training and helps gradient descent converge faster.</li> </ul> </li> <li><strong>Normalize brightness</strong>: Eliminates the common bias of “brightness” shared across images, allowing the model to focus on <strong>structural differences</strong>.</li> </ul> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># third: append the bias dimension of ones (i.e. bias trick) so that our classifier
# only has to worry about optimizing a single weight matrix W.
</span><span class="n">X_train</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">hstack</span><span class="p">([</span><span class="n">X_train</span><span class="p">,</span> <span class="n">np</span><span class="p">.</span><span class="nf">ones</span><span class="p">((</span><span class="n">X_train</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="mi">1</span><span class="p">))])</span>
<span class="n">X_val</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">hstack</span><span class="p">([</span><span class="n">X_val</span><span class="p">,</span> <span class="n">np</span><span class="p">.</span><span class="nf">ones</span><span class="p">((</span><span class="n">X_val</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="mi">1</span><span class="p">))])</span>
<span class="n">X_test</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">hstack</span><span class="p">([</span><span class="n">X_test</span><span class="p">,</span> <span class="n">np</span><span class="p">.</span><span class="nf">ones</span><span class="p">((</span><span class="n">X_test</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="mi">1</span><span class="p">))])</span>
<span class="n">X_dev</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">hstack</span><span class="p">([</span><span class="n">X_dev</span><span class="p">,</span> <span class="n">np</span><span class="p">.</span><span class="nf">ones</span><span class="p">((</span><span class="n">X_dev</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="mi">1</span><span class="p">))])</span>
</code></pre></div></div> <p>Here is an important trick for handling the bias term $b$. Recall the linear score function: \(f(x_i, W, b) = W x_i + b\) Computating $W$ and $b$ separately requires maintaining two distinct parameters (matrices and vectors), which complicates the implementation of gradients and updates.</p> <p>We can use the <strong>Bias Trick</strong> to simplify this:</p> \[\mathbf{x}' = \begin{bmatrix} \mathbf{x} \\ 1 \end{bmatrix} \in \mathbb{R}^{D+1}, \qquad \mathbf{W}' = \begin{bmatrix} \mathbf{W} &amp; \mathbf{b} \end{bmatrix} \in \mathbb{R}^{C \times (D+1)}\] <p><em>(Note: Dimensions may transpose depending on implementation. In our code, $X$ is $N \times D$, so we append 1 horizontally)</em></p> \[X' = \begin{bmatrix} X &amp; \mathbf{1} \end{bmatrix} \in \mathbb{R}^{N \times (D+1)}, \quad W' = \begin{bmatrix} W \\ b^\top \end{bmatrix} \in \mathbb{R}^{(D+1) \times C}\] \[f(X') = X'W' = XW + \mathbf{1}b^\top = XW + b \in \mathbb{R}^{N \times C}\] <p>By appending a column of ones to $X$, we absorb the bias $b$ into the weight matrix $W$. Now we only need to maintain a single matrix $W$.</p> <hr/> <h1 id="2-implementing-softmax_loss_naive">2. Implementing softmax_loss_naive</h1> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">#####################################################################
# TODO:
# Compute the gradient of the loss function and store it dW.
# Rather that first computing the loss and then computing the 
# derivative,      
# it may be simpler to compute the derivative at the same time that 
# the loss is being computed. As a result you may need to modify some 
# of the code above to compute the gradient.
#####################################################################
</span></code></pre></div></div> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">softmax_loss_naive</span><span class="p">(</span><span class="n">W</span><span class="p">,</span> <span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">reg</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Softmax loss function, naive implementation (with loops)
    Inputs have dimension D, there are C classes, and we operate on minibatches
    of N examples.
    
    Inputs:
    - W: A numpy array of shape (D, C) containing weights.
    - X: A numpy array of shape (N, D) containing a minibatch of data.
    - y: A numpy array of shape (N,) containing training labels; y[i] = c means
         that X[i] has label c, where 0 &lt;= c &lt; C.
    - reg: (float) regularization strength
      
    Returns a tuple of:
    - loss as single float
    - gradient with respect to weights W; an array of same shape as W
    </span><span class="sh">"""</span>
    <span class="c1"># Initialize the loss and gradient to zero.
</span>    <span class="n">loss</span> <span class="o">=</span> <span class="mf">0.0</span>
    <span class="n">dW</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros_like</span><span class="p">(</span><span class="n">W</span><span class="p">)</span>
    
    <span class="c1"># compute the loss and the gradient
</span>    <span class="n">num_classes</span> <span class="o">=</span> <span class="n">W</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span>
    <span class="n">num_train</span> <span class="o">=</span> <span class="n">X</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_train</span><span class="p">):</span>
        <span class="n">scores</span> <span class="o">=</span> <span class="n">X</span><span class="p">[</span><span class="n">i</span><span class="p">].</span><span class="nf">dot</span><span class="p">(</span><span class="n">W</span><span class="p">)</span>  <span class="c1"># shape: (C,)
</span>        
        <span class="c1"># compute the probabilities in a numerically stable way
</span>        <span class="c1"># shift values for 'exp' to avoid numeric overflow
</span>        <span class="n">scores</span> <span class="o">-=</span> <span class="n">np</span><span class="p">.</span><span class="nf">max</span><span class="p">(</span><span class="n">scores</span><span class="p">)</span>
        
        <span class="c1"># Softmax Function
</span>        <span class="n">p</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">scores</span><span class="p">)</span>
        <span class="n">p</span> <span class="o">/=</span> <span class="n">p</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span> <span class="c1"># normalize
</span>        <span class="n">logp</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">p</span><span class="p">)</span>
        <span class="n">loss</span> <span class="o">-=</span> <span class="n">logp</span><span class="p">[</span><span class="n">y</span><span class="p">[</span><span class="n">i</span><span class="p">]]</span> <span class="c1"># negative log probability is the loss
</span>        
        <span class="c1"># My Code
</span>        <span class="n">p</span><span class="p">[</span><span class="n">y</span><span class="p">[</span><span class="n">i</span><span class="p">]]</span> <span class="o">-=</span> <span class="mi">1</span>
        <span class="n">dW</span> <span class="o">+=</span> <span class="n">np</span><span class="p">.</span><span class="nf">outer</span><span class="p">(</span><span class="n">X</span><span class="p">[</span><span class="n">i</span><span class="p">],</span> <span class="n">p</span><span class="p">)</span>
    
    <span class="c1"># normalized hinge loss plus regularization
</span>    <span class="n">loss</span> <span class="o">=</span> <span class="n">loss</span> <span class="o">/</span> <span class="n">num_train</span> <span class="o">+</span> <span class="n">reg</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">W</span> <span class="o">*</span> <span class="n">W</span><span class="p">)</span>
    
    <span class="c1"># My Code
</span>    <span class="n">dW</span> <span class="o">=</span> <span class="n">dW</span> <span class="o">/</span> <span class="n">num_train</span> <span class="o">+</span> <span class="mi">2</span> <span class="o">*</span> <span class="n">reg</span> <span class="o">*</span> <span class="n">W</span>
    
    <span class="k">return</span> <span class="n">loss</span><span class="p">,</span> <span class="n">dW</span>
</code></pre></div></div> <p>Before implementing the gradient code, let’s derive the gradient of the <strong>Softmax Loss</strong>.</p> <p>The full loss function including regularization is:</p> \[L(W) = \frac{1}{N}\sum_{i=1}^{N} L_i(x_i, y_i, W) + \lambda R(W)\] <p>Where for a single example $x_i$:</p> <ul> <li>Scores: $f = X_i W$ (where $f_k = X_i W_k$)</li> <li>Softmax Probability: $p_k = \frac{e^{f_k}}{\sum_j e^{f_j}}$</li> <li>Loss: $L_i = -\log(p_{y_i})$</li> </ul> <hr/> <h2 id="21-analytical-gradient-derivation">2.1 Analytical Gradient Derivation</h2> <p>Our goal is to find $\frac{\partial L_i}{\partial W}$. We use the chain rule: \(\frac{\partial L_i}{\partial W} = \frac{\partial L_i}{\partial f} \frac{\partial f}{\partial W}\)</p> <h3 id="step-1-derivative-wrt-scores-f_k"><mark>Step 1: Derivative w.r.t Scores ($f_k$)</mark></h3> <p>Let $k$ be any class index. We want $\frac{\partial L_i}{\partial f_k}$.</p> <p>Since $L_i = -\log(p_{y_i})$, we differentiating $-\log(\frac{e^{f_{y_i}}}{\sum e^{f_j}})$.</p> <p><em><strong>Case 1</strong></em> : $k = y_i$ (Correct Class)</p> \[\begin{aligned} \frac{\partial L_i}{\partial f_{y_i}} &amp;= \frac{\partial}{\partial f_{y_i}} (-\log p_{y_i}) = -\frac{1}{p_{y_i}} \frac{\partial p_{y_i}}{\partial f_{y_i}} \\ &amp;= -\frac{1}{p_{y_i}} [p_{y_i}(1-p_{y_i})] = -(1-p_{y_i}) = p_{y_i} - 1 \end{aligned}\] <p><br/></p> <p><em><strong>Case 2</strong></em> : $k \neq y_i$ (Incorrect Class)</p> \[\begin{aligned} \frac{\partial L_i}{\partial f_k} &amp;= \frac{\partial}{\partial f_k} (-\log p_{y_i}) = -\frac{1}{p_{y_i}} \frac{\partial p_{y_i}}{\partial f_k} \\ &amp;= -\frac{1}{p_{y_i}} [-p_{y_i}p_k] = p_k \end{aligned}\] <p><br/></p> <p><em><strong>Combine Cases</strong></em> : We can combine these into a single expression using <strong>the indicator function</strong> $\mathbb{1}(k=y_i)$:</p> \[\frac{\partial L_i}{\partial f_k} = p_k - \mathbb{1}(k=y_i)\] <p>This is simply the <strong>probability vector minus the one-hot label vector</strong>. Let $\delta = p - y_{\text{onehot}}$. Then $\frac{\partial L_i}{\partial f} = \delta$.</p> <p><br/></p> <h3 id="step-2-derivative-wrt-weights-w"><mark>Step 2: Derivative w.r.t Weights ($W$)</mark></h3> <p>The score for class $k$ is $f_k = x_i \cdot W_k$ (dot product).</p> <p>So, $\frac{\partial f_k}{\partial W_{d,k}} = x_{i,d}$. (Note: $f_k$ only depends on the $k$-th column of $W$).</p> <p>Thus, for a specific weight element $W_{d,k}$:</p> \[\frac{\partial L_i}{\partial W_{d,k}} = \frac{\partial L_i}{\partial f_k} \frac{\partial f_k}{\partial W_{d,k}} = (p_k - \mathbb{1}(k=y_i)) x_{i,d}\] <p><br/></p> <h3 id="step-3-vectorized-gradient"><mark>Step 3: Vectorized Gradient</mark></h3> <p>Collecting this for all $d, k$, we get the outer product: \(\nabla_W L_i = x_i^T (p - y_{\text{onehot}})\)</p> <p><em>(Dimension check: $x_i^T$ is $(D,1)$, $(p-y)$ is $(1,C)$ $\rightarrow$ $(D,C)$ matrix)</em>.</p> <hr/> <h2 id="22-final-gradient-formula">2.2 Final Gradient Formula</h2> <p>Averaging over $N$ examples and adding regularization:</p> \[\nabla_W L(W) = \frac{1}{N} \sum_{i=1}^{N} x_i^T (p_i - y_i^{\text{onehot}}) + 2\lambda W\] <hr/> <h2 id="23-evaluation">2.3 Evaluation</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Evaluate the naive implementation of the loss we provided for you:
</span><span class="kn">from</span> <span class="n">cs231n.classifiers.softmax</span> <span class="kn">import</span> <span class="n">softmax_loss_naive</span>
<span class="kn">import</span> <span class="n">time</span>

<span class="c1"># generate a random Softmax classifier weight matrix of small numbers
</span><span class="n">W</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="mi">3073</span><span class="p">,</span> <span class="mi">10</span><span class="p">)</span> <span class="o">*</span> <span class="mf">0.0001</span>

<span class="n">loss</span><span class="p">,</span> <span class="n">grad</span> <span class="o">=</span> <span class="nf">softmax_loss_naive</span><span class="p">(</span><span class="n">W</span><span class="p">,</span> <span class="n">X_dev</span><span class="p">,</span> <span class="n">y_dev</span><span class="p">,</span> <span class="mf">0.000005</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">loss: %f</span><span class="sh">'</span> <span class="o">%</span> <span class="p">(</span><span class="n">loss</span><span class="p">,</span> <span class="p">))</span>

<span class="c1"># As a rough sanity check, our loss should be something close to -log(0.1).
</span><span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">loss: %f</span><span class="sh">'</span> <span class="o">%</span> <span class="n">loss</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">sanity check: %f</span><span class="sh">'</span> <span class="o">%</span> <span class="p">(</span><span class="o">-</span><span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="mf">0.1</span><span class="p">)))</span>
</code></pre></div></div> <p><br/></p> <h2 id="24-inline-question-1">2.4 Inline Question 1</h2> <blockquote> <p><mark>Why do we expect our loss to be close to -log(0.1)?</mark></p> <details> <summary><b>Answer</b></summary> Since $W$ is initialized with very small random numbers ($\approx 0$), the scores $f = XW$ will be approximately $0$. Consequently, the Softmax probabilities will be uniform across all classes: $$ p_k = \frac{e^{f_k}}{\sum_j e^{f_j}} \approx \frac{e^0}{\sum_{j=1}^{10} e^0} = \frac{1}{10} = 0.1 $$ The loss for each example is $Li = -\log(p_{y_i}) \approx -\log(0.1)$. </details> </blockquote>]]></content><author><name></name></author><category term="AI"/><category term="Computer Vision"/><category term="cs231n"/><category term="softmax"/><category term="cv"/><summary type="html"><![CDATA[A walkthrough of implementing a Softmax Classifier for CS231n Assignment 1, including preprocessing, loss function derivation, and gradient computation.]]></summary></entry></feed>